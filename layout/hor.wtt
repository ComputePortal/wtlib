import {FlexStyle} from "./FlexStyle.wtt"

export template hor(type, style={}) extends div super(style=FlexStyle("h" + $type) + $style)

export template ver(type, style={}) extends div super(style=FlexStyle("v" + $type) + $style)
