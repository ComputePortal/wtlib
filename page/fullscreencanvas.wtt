import [empty] from="./empty.wtt"

export template fullscreencanvas(title=null, description=null) extends=empty super()
  append head
    meta(name=viewport, content="width=device-width, initial-scale=1.0")

  canvas(id=canvas, style={height: 100%, width: 100%, position: absolute})
